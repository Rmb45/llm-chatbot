import requests\nfrom typing import Optional, Dict, List\nimport random\n\nclass JokeGenerator:\n    \"\"\"Fetches and manages jokes from external APIs.\"\"\"\n    \n    # API endpoints\n    JOKE_API_URL = \"https://official-joke-api.appspot.com/random_joke\"\n    JOKES_BATCH_URL = \"https://official-joke-api.appspot.com/jokes/{joke_type}/random\"\n    ALL_JOKES_URL = \"https://official-joke-api.appspot.com/jokes/random\"\n    JOKE_TYPES_URL = \"https://official-joke-api.appspot.com/types\"\n    \n    def __init__(self, timeout: int = 5):\n        \"\"\"\n        Initialize the JokeGenerator.\n        \n        Args:\n            timeout: Request timeout in seconds\n        \"\"\"\n        self.timeout = timeout\n        self.session = requests.Session()\n    \n    def get_random_joke(self) -> Optional[Dict]:\n        \"\"\"\n        Fetch a single random joke.\n        \n        Returns:\n            Dictionary with 'setup' and 'punchline' keys, or None if request fails\n        \"\"\"\n        try:\n            response = self.session.get(self.JOKE_API_URL, timeout=self.timeout)\n            response.raise_for_status()\n            joke = response.json()\n            return {\n                'setup': joke.get('setup', ''),\n                'punchline': joke.get('punchline', ''),\n                'type': joke.get('type', 'general'),\n                'id': joke.get('id', 'unknown')\n            }\n        except requests.RequestException as e:\n            print(f\"Error fetching joke: {e}\")\n            return None\n    \n    def get_random_jokes_batch(self, count: int = 5) -> List[Dict]:\n        \"\"\"\n        Fetch multiple random jokes.\n        \n        Args:\n            count: Number of jokes to fetch\n            \n        Returns:\n            List of joke dictionaries\n        \"\"\"\n        jokes = []\n        for _ in range(count):\n            joke = self.get_random_joke()\n            if joke:\n                jokes.append(joke)\n        return jokes\n    \n    def get_joke_by_type(self, joke_type: str) -> Optional[Dict]:\n        \"\"\"\n        Fetch a joke of a specific type.\n        \n        Args:\n            joke_type: Type of joke ('general', 'knock-knock', 'programming', etc.)\n            \n        Returns:\n            Dictionary with joke details, or None if request fails\n        \"\"\"\n        try:\n            url = self.JOKES_BATCH_URL.format(joke_type=joke_type)\n            response = self.session.get(url, timeout=self.timeout)\n            response.raise_for_status()\n            \n            # Handle both single joke and list response\n            data = response.json()\n            if isinstance(data, list):\n                joke = data[0] if data else None\n            else:\n                joke = data\n            \n            if joke:\n                return {\n                    'setup': joke.get('setup', ''),\n                    'punchline': joke.get('punchline', ''),\n                    'type': joke.get('type', joke_type),\n                    'id': joke.get('id', 'unknown')\n                }\n            return None\n        except requests.RequestException as e:\n            print(f\"Error fetching {joke_type} joke: {e}\")\n            return None\n    \n    def get_available_types(self) -> List[str]:\n        \"\"\"\n        Get all available joke types from the API.\n        \n        Returns:\n            List of available joke types\n        \"\"\"\n        try:\n            response = self.session.get(self.JOKE_TYPES_URL, timeout=self.timeout)\n            response.raise_for_status()\n            return response.json()\n        except requests.RequestException as e:\n            print(f\"Error fetching joke types: {e}\")\n            return ['general', 'knock-knock', 'programming']\n    \n    def format_joke(self, joke: Dict, include_type: bool = False, include_id: bool = False) -> str:\n        \"\"\"\n        Format a joke for display.\n        \n        Args:\n            joke: Joke dictionary with 'setup' and 'punchline'\n            include_type: Whether to include joke type\n            include_id: Whether to include joke ID\n            \n        Returns:\n            Formatted joke string\n        \"\"\"\n        if not joke:\n            return \"No joke available.\"\n        \n        formatted = f\"{joke.get('setup', '')}\\n{joke.get('punchline', '')}\"\n        \n        if include_type:\n            formatted += f\"\\n[Type: {joke.get('type', 'unknown')}]\"\n        \n        if include_id:\n            formatted += f\" [ID: {joke.get('id', 'unknown')}]\"\n        \n        return formatted\n    \n    def close(self):\n        \"\"\"Close the session.\"\"\"\n        self.session.close()\n    \n    def __enter__(self):\n        \"\"\"Context manager entry.\"\"\"\n        return self\n    \n    def __exit__(self, exc_type, exc_val, exc_tb):\n        \"\"\"Context manager exit.\"\"\"\n        self.close()\n"